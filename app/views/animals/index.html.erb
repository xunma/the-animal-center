<h1>The Cute Animals waiting to be adopted</h1>
<ul>

  <% @animals.each do |animal| %>
    <li><%= "#{animal.name} | #{animal.species}" %></li>
    <%= link_to "learn more", animal_path(animal) %>
  <% end %>
</ul>
